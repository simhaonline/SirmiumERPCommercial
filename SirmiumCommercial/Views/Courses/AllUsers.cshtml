@model CourseViewModel

@if (Model.Course.Users == null)
{
    <div class="col-lg-12 navbar bg-info">

        <span class="navbar-brand">No Users</span>

    </div>
}
else
{
    @await Component.InvokeAsync("CourseUsers", new { courseId = Model.Course.CourseId })
}